---
title: "Требования к серверу"
translation: "getting-started/server-requirements"
---

MODX отлично работает на большинстве виртуальных/облачных хостингов, а также на VPS и выделенных серверах. MODX написан на PHP, обычно использует базу данных MySQL и нуждается в веб-сервере, таком как Apache или NGINX, для обслуживания веб-запросов.

| Компонент | Минимум | Рекомендация |
| --------- | ------- | ------------ |
| PHP | 8.1 | 8.2 или выше |
| Database | Последняя MySQL 5.6.x | MariaDB 10.1.x или Percona Server 5.6.x и выше |
| Webserver | * | NGINX 1.18 или Apache 2.4 |

## PHP

Для MODX 3 нужен **PHP 8.1 или выше** (`composer.json` в [ветке 3.x](https://github.com/modxcms/revolution/blob/3.x/composer.json)). Для новых установок берите PHP 8.2+.

### Обязательные расширения

Эти расширения PHP требует ядро (тот же список `ext-*`, что в `composer.json`):

- `curl`
- `dom`
- `fileinfo`
- `gd`
- `json`
- `pdo`
- `simplexml`
- `xml`
- `xmlwriter`
- `zip`
- `zlib`

Для MySQL ещё нужен драйвер PDO MySQL (`pdo_mysql`). Setup проверяет его, когда вы выбираете драйвер MySQL.

На многих хостингах это пакеты вроде `php-xml` (покрывает `dom`, `simplexml`, `xml`, `xmlwriter`), `php-gd`, `php-curl`, `php-zip` и `php-mysql` / `php-pdo`.

### Рекомендуемые расширения

Composer также *предлагает* их. Для голой установки они не обязательны, но на практике почти всегда нужны:

- `mbstring` — при включённом `use_multibyte` и во многих дополнениях
- `iconv` — встроенная транслитерация псевдонимов
- `imagick` — более сильная обработка изображений, чем один `gd`
- `intl` — интернационализация

Рекомендуется `memory_limit` не ниже 64M.

## База данных

MODX поддерживает базу `mysql`. Есть сторонняя реализация `postgres`. Важно: дополнения тоже должны реализовать драйверы для своих таблиц, и чаще всего это делают только для `mysql`, поэтому MySQL — самый надёжный выбор.

> Примечание: поддержка sqlsrv устарела и [удалена в 3.0](https://github.com/modxcms/revolution/issues/15540).

Минимальная поддерживаемая версия MySQL — 4.1.20, рекомендуется 5.7 или выше. Можно использовать кластеры вроде Galera.

Поддерживаются механизмы хранения MyISAM и InnoDB, а также наборы символов utf8 и utf8mb. Для широкой поддержки UTF-8 лучше utf8mb.

Требуются следующие разрешения:

- `SELECT`, `INSERT`, `UPDATE`, `DELETE` — для обычной работы
- `CREATE`, `ALTER`, `INDEX`, `DROP` — для установки и обновления ядра и дополнений
- `CREATE TEMPORARY TABLES` — некоторыми сторонними дополнениями

## Веб-серверы

MODX работает на большинстве современных веб-серверов. Рекомендуются Apache 2.4+ или nginx 1.18.x.

Чтобы использовать [friendly urls](getting-started/friendly-urls), может понадобиться дополнительная настройка. Инструкции есть для [apache](getting-started/friendly-urls/apache), [nginx](getting-started/friendly-urls/nginx) и [lighttpd](getting-started/friendly-urls/lighttpd).

## Поддержка браузера для Менеджера

Для интерфейса менеджера поддерживаются такие браузеры на десктопе:

- Edge (последние 2 версии)
- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)

Мобильные и планшетные:

- Chrome for Android (последний)
- Safari on iOS (последний)

Менеджер может работать и в более старых браузерах, но официально они не поддерживаются.

Эти требования только для менеджера. Какие браузеры поддерживает ваш сайт, решаете вы.
